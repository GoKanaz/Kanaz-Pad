name: Android CI

on:
  push:
    branches: ["disabled"]
  pull_request:
    branches: ["disabled"]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - run: echo "CI disabled - fix project structure first"
